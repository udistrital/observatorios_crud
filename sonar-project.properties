# Required metadata
sonar.projectKey=observatorios_crud
sonar.projectName=observatorios_crud
sonar.projectVersion=1.0

# Comma-separated paths to directories with sources (required)
sonar.sources=osiris

sonar.language=py

# Encoding of the source files
sonar.sourceEncoding=UTF-8

sonar.exclusions=**/__pycache__/**,**/migrations/**,**/*.pyc,**/*.md,**/tests/**